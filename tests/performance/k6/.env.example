# Copy this file to .env and set real values there.
# Never commit tests/performance/k6/.env

# Base API URL from inside Docker network (default when using docker compose k6 service)
API_BASE_URL=http://gateway/api/v1

# Credentials used by k6 login flow
K6_USERNAME=sample
K6_PASSWORD=sample
